import { css } from 'styled-system/css';

export const accountDetailContainerStyles = css({
  overflow: 'auto'
});

export const accountDetailDisplayStyles = css({
  display: 'flex',
  flexDirection: 'row',
  gap: 5
});

export const flexColumnStyles = css({
  display: 'flex',
  flexDirection: 'column',
  gap: 5
});
